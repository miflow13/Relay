[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "relaylab"
version = "0.1.0"
description = "A local-first two-agent software development experiment powered by Ollama."
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
include = ["relaylab*"]
